# AGENTS.md - {Project Name}

## Standards

This project follows [Doctrine](https://github.com/agh/doctrine) for all coding standards:

| Concern | Guide |
|---------|-------|
| {Language} | [guides/languages/{language}.md](https://github.com/agh/doctrine/blob/main/guides/languages/{language}.md) |
| {Framework} | [guides/frameworks/{framework}.md](https://github.com/agh/doctrine/blob/main/guides/frameworks/{framework}.md) |
| Testing | [guides/process/testing.md](https://github.com/agh/doctrine/blob/main/guides/process/testing.md) |
| CI/CD | [guides/process/ci.md](https://github.com/agh/doctrine/blob/main/guides/process/ci.md) |

**Do not duplicate Doctrine guidance here.** This file contains only project-specific context.

---

## Overview

{Brief description of what this project does, its purpose, and key technologies.}

**Stack:** {Language} {version}, {Framework} {version}, {Database} {version}

## Commands

```bash
# Development
{install_command}
{dev_command}

# Testing
{test_command}

# Linting & Formatting
{lint_command}
```

## Structure

```
{project}/
├── src/              # {description}
├── tests/            # {description}
└── ...
```

**Key Files:**
- `{file}` — {purpose}
- `{file}` — {purpose}

## Architecture

{Brief architecture description or diagram reference}

**Patterns:**
- **{Pattern}**: {Why we use it in this project}

## Project-Specific Conventions

{Only document conventions that DIFFER from or EXTEND Doctrine standards}

- {Convention unique to this project}
- {Convention unique to this project}

## Common Pitfalls

{Document gotchas specific to THIS codebase, not general language/framework issues}

### {Pitfall}

**Problem**: {What happens}
**Solution**: {How to fix}

## Environment

```bash
# Required
{VAR}={example}

# Optional
{VAR}={example}  # {description}
```

## External Dependencies

| Service | Purpose | Docs |
|---------|---------|------|
| {Service} | {Purpose} | {Link} |

---

## Tool Compatibility

Codex, GitHub Copilot and Cursor read this file directly. Tools with a
different default filename need a link or an import:

```bash
ln -s AGENTS.md CLAUDE.md   # Claude Code; or a CLAUDE.md holding @AGENTS.md
ln -s AGENTS.md GEMINI.md   # Gemini CLI; or add AGENTS.md to context.fileName
```

Aider discovers no file. Load this one with `aider --read AGENTS.md`, or set
`read: AGENTS.md` in `.aider.conf.yml`.
